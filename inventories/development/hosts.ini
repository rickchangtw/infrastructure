# Development inventory (ONE-12 / ONE-14)
#
# This inventory is ONLY for the Development host. Production hosts must never
# be added here; see inventories/production/hosts.ini and the site.yml
# host-gating comment in playbooks/site.yml (production uses the
# discourse_prod group, which this inventory never defines).
#
# Development host: 100.94.136.15 (pollux). Authorized by ONE-12 A1 gate.
# All roles target the dedicated Discourse service only. Existing
# Paperclip/Hermes/SmartCondo/monitoring services are never touched.

[discourse_dev]
dev-dc1 ansible_host=100.94.136.15 ansible_user=deploy

[discourse:children]
discourse_dev

# Deliberately NOT defined here. Production is never reachable via the
# development inventory, so `ansible-playbook -i inventories/development/hosts.ini`
# can never touch the production group.
