# Discourse Redis env file (rendered by Ansible onto the host, chmod 0600).
# SECRET VALUES injected from the vault at deploy time; never committed.
DISCOURSE_REDIS_PASSWORD={{ discourse_redis_password }}
DISCOURSE_REDIS_DB={{ discourse_redis_db }}
